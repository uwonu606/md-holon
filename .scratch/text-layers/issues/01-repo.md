# 새 저장소를 연다
Type: task
Status: resolved
Blocked by:

## Question

저장소 이름과 자리를 정하고 `git init` 한다. 이 map 과 `docs/research/` 의 조사 파일, `docs/agents/issue-tracker.md` 를 거기로 옮긴다. 이름은 사용자가 정한다.

## Answer

이름은 md-holon, 자리는 `~/workspace/alt/md-holon`. 2026-09-14 `git init` 했고 map(`.scratch/text-layers/`), 조사 둘(`docs/research/`), tracker 설정(`docs/agents/issue-tracker.md`)을 docguard 워크트리에서 옮겨 왔다. holon 은 그 자체로 온전하면서 더 큰 전체의 부분인 것이라, 계층 하나하나가 그렇다는 뜻이다. md 는 안의 불변 조건이다. 긁어 온 순간 md 가 되고 그 뒤 검사·계층·충돌 장이 다 md 위에서 돈다.
